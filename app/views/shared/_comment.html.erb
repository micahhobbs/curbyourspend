<%= simple_form_for([@item, @comment], remote: true) do |f| %>
  <%= f.input :comment, placeholder: "Enter comment", label: false, style:'height: 100px;' %>
    <div class='d-grid col-8 justify-content-end'>
      <%= f.submit class: "btn othr-btn btn-lg mb-2" %>
    </div>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
<% end %>
