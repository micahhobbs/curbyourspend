 <%= simple_form_for([@item, @comment], remote: true) do |f| %>
    <%= f.input :comment, placeholder: "Enter comment", label: false %>
    <%= f.submit class: "btn gen-btn btn-lg mb-2" %>
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
<% end %>
