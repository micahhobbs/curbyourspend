<ul class="nav navbar bg-white border-top border-bottom py-3 justify-content-end px-5">
  <div class="container d-flex justify-content-end">
    <li class="nav-item px-1">
      <%= link_to 'Back to Item', item_path(@item), class: 'btn btn-secondary' %>
    </li>
    <li class="nav-item px-1">
      <%= link_to 'My Items', items_path, class: 'btn btn-secondary' %>
    </li>
    <li class="nav-item px-1">
      <%= link_to 'Delete', item_path(@item), class: 'btn btn-secondary', method: :delete, data: { confirm: 'Are you sure you want to delete this item?' } %>
    </li>
  </div>
</ul>

<div class="row py-5">
  <div class="col-6 container mt-3 py-3 border bg-white">
    <div class='container text-left my-4'>
      <h1 class='d-inline'>Edit item</h1>
    </div>
    <div class='text-center px-5'>
      <%= render partial: 'shared/form', item: @item %>
    </div>
  </div>
</div>
