<div class='container py-3'>
  <h1 class='d-inline'>Your Items</h1>
  <%= link_to 'Add new item', new_item_path, class: 'btn gen-btn d-inline mx-2' %>
</div>
<div class='container'>
  <table class='table table-bordered table-hover all-items'>
    <thead>
      <tr>
        <th scope="col">Item</th>
        <th scope="col">Value</th>
        <th scope="col">Reason</th>
        <th scope="col">Description</th>
        <th scope="col">Link</th>
        <th class='date-col' scope="col">Start Date</th>
        <th class='date-col' scope="col">End Date</th>
        <th scope="col">Action</th>
     </tr>
    </thead>
    <tbody>
      <%  @items.each do |item| %>
        <% if item.user_id == current_user.id %>
          <tr onclick="location.href='<%= item_path(item.id) %>'">
            <td><%= item.name %></td>
            <td><%= number_to_currency(item.value) %></td>
            <td><%= item.reason %></td>
            <td><%= item.description %></td>
            <td><%= link_to item.link, item.link %></td>
            <td class='date-col'><%= item.start_date %></td>
            <td class='date-col'><%= item.end_date %></td>
            <td>
              <div class='d-inline-flex'>
                <%= link_to 'Edit', edit_item_path(item), class: 'btn wht-btn mx-2' %><%= link_to 'Delete', item_path(item), class: 'btn wht-btn', method: :delete, data: { confirm: 'Are you sure you want to delete this item?' } %>
              </div>
            </td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
